def func(int num) -> int {
    int sum = 0;
    for(int i=0; i<num ; i=i+1){
        if((i%3 == 0) or (i%5 == 0)){
            sum=sum+i;
        }
    }
    yeet sum
}

yap(func(100)); # Expected: 2318